int get_gross_pay(int hours =30, int pay_rate=20); //if we send two parameters it will use this one
int get_gross_pay(int hours , double pay_rate); //this works as well
int get_gross_pay(int hours, int pay_rate, double sales); // if we send three parameters it will use this one